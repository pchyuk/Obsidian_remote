# 자료구조 유형별 정리

## 개요

- 마지막 업데이트: 2026-05-22 21:00 KST
- 누적 정리 수: 20개
- 출처: 매일 정처기/전산직 학습 cron 요약

---

## 2026-04

### 2026-04-27 09:01 - 자료구조 - 스택과 큐

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 스택과 큐

1. 핵심 요약
- 스택(Stack)은 데이터를 한쪽 끝에서만 넣고 빼는 선형 자료구조입니다.
- 가장 나중에 들어온 데이터가 가장 먼저 나가므로 후입선출, 즉 LIFO 구조라고 합니다.
- 접시를 쌓았다가 위에서부터 꺼내는 모습을 떠올리면 이해하기 쉽습니다.
- 큐(Queue)는 한쪽에서는 삽입, 반대쪽에서는 삭제가 일어나는 선형 자료구조입니다.
- 가장 먼저 들어온 데이터가 가장 먼저 나가므로 선입선출, 즉 FIFO 구조입니다.
- 줄 서기처럼 먼저 온 요청을 먼저 처리해야 하는 상황에 잘 맞습니다.
- 스택은 함수 호출, 재귀, 괄호 검사, 뒤로 가기 기능에 자주 쓰입니다.
- 큐는 작업 대기열, 프린터 출력 순서, 버퍼, BFS 탐색에서 자주 활용됩니다.

2. 자주 나오는 포인트
- 스택: LIFO, push/pop, top 개념을 묻는 문제가 자주 나옵니다.
- 큐: FIFO, enqueue/dequeue, front/rear 개념을 구분해야 합니다.
- BFS는 큐, DFS는 스택 또는 재귀와 연결해서 기억하면 좋습니다.

3. OX 문제
스택은 후입선출(LIFO) 구조라서 재귀 호출, 함수 호출 복귀 주소 관리, 괄호 검사에 활용되고, 큐는 선입선출(FIFO) 구조라서 작업 대기열이나 BFS 탐색에 활용된다.
O / X?

### 2026-04-28 13:00 - 자료구조 - 이진 트리와 순회

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 이진 트리와 순회

1. 핵심 요약
- 트리는 계층 구조를 표현하는 비선형 자료구조입니다.
- 이진 트리는 각 노드가 최대 2개의 자식 노드를 가지는 트리입니다.
- 자식은 보통 왼쪽 자식, 오른쪽 자식으로 구분합니다.
- 트리 순회는 모든 노드를 빠짐없이 한 번씩 방문하는 방법입니다.
- 전위 순회는 루트 → 왼쪽 → 오른쪽 순서입니다.
- 중위 순회는 왼쪽 → 루트 → 오른쪽 순서입니다.
- 후위 순회는 왼쪽 → 오른쪽 → 루트 순서입니다.
- 이름에서 “전/중/후”는 루트 노드를 언제 방문하는지를 기준으로 보면 쉽습니다.

2. 자주 나오는 포인트
- 전위 순회: 루트가 가장 먼저 나온다.
- 중위 순회: 이진 탐색 트리에서 사용하면 값이 오름차순으로 나올 수 있다.
- 후위 순회: 하위 노드를 먼저 처리하므로 수식 트리 계산, 디렉터리 삭제 등에 응용된다.

3. OX 문제
이진 트리의 중위 순회는 루트 노드를 먼저 방문한 뒤 왼쪽 서브트리와 오른쪽 서브트리를 차례로 방문하는 방식이다.
O / X?

### 2026-04-29 16:00 - 자료구조 - 그래프 탐색 DFS와 BFS

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 그래프 탐색 DFS와 BFS

1. 핵심 요약
- 그래프는 정점과 간선으로 구성된 자료구조입니다.
- DFS는 깊이 우선 탐색으로, 한 방향으로 갈 수 있을 만큼 깊게 들어간 뒤 되돌아옵니다.
- DFS는 보통 스택이나 재귀 호출로 구현합니다.
- BFS는 너비 우선 탐색으로, 시작 정점에서 가까운 정점부터 차례로 방문합니다.
- BFS는 먼저 들어온 정점을 먼저 처리해야 하므로 큐를 사용합니다.
- 무가중치 그래프에서 BFS는 시작점으로부터 간선 수가 가장 적은 경로를 먼저 찾을 수 있어 최단 경로 문제에 자주 쓰입니다.
- DFS는 경로 존재 여부, 사이클 탐지, 백트래킹 문제와 자주 연결됩니다.
- 시험에서는 “DFS=스택/재귀”, “BFS=큐”, “BFS=무가중치 최단 경로”를 연결해서 기억하면 좋습니다.

2. 자주 나오는 포인트
- DFS: 스택 또는 재귀, 깊게 탐색, 백트래킹과 관련
- BFS: 큐, 가까운 정점부터 탐색, 무가중치 최단 경로에 활용
- 방문 체크를 하지 않으면 사이클이 있는 그래프에서 무한 반복될 수 있음

3. OX 문제
그래프 탐색에서 BFS는 큐를 이용해 시작 정점에서 가까운 정점부터 방문하므로, 간선 가중치가 없는 그래프에서 최단 경로 탐색에 활용될 수 있다.
O / X?

### 2026-04-30 12:00 - 자료구조 - 선택 정렬과 시간복잡도

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 선택 정렬과 시간복잡도

1. 핵심 요약
- 선택 정렬은 정렬되지 않은 구간에서 가장 작은 값 또는 가장 큰 값을 선택해 앞쪽부터 차례로 배치하는 정렬 방식입니다.
- 예를 들어 오름차순 정렬이라면, 첫 번째 회차에는 전체에서 최솟값을 찾아 맨 앞 원소와 교환합니다.
- 두 번째 회차에는 두 번째 위치부터 끝까지 중 최솟값을 찾아 두 번째 위치에 놓습니다.
- 이렇게 정렬된 구간은 앞에서부터 하나씩 늘어나고, 정렬되지 않은 구간은 줄어듭니다.
- 핵심은 “비교는 많이 하지만, 교환은 비교적 적다”는 점입니다.
- 선택 정렬은 입력이 이미 정렬되어 있어도 최솟값을 찾기 위해 계속 비교해야 하므로 최선·평균·최악 시간복잡도가 모두 O(n²)입니다.
- 구현은 단순하지만 데이터가 많을수록 비효율적이라 실무보다는 시험용 기본 정렬 개념으로 자주 나옵니다.

2. 자주 나오는 포인트
- 선택 정렬: 남은 원소 중 최솟값/최댓값을 선택해 제자리 교환
- 시간복잡도: 최선·평균·최악 모두 O(n²)
- 버블 정렬·삽입 정렬과 함께 단순 정렬 계열로 비교 출제 자주 나옴

3. OX 문제
선택 정렬은 매 회차마다 남은 원소 중 최솟값을 찾아 앞쪽 원소와 교환하는 방식이며, 평균 시간복잡도는 O(n log n)이다.
O / X?

---

## 2026-05

### 2026-05-02 16:00 - 자료구조 - 해시 테이블과 충돌 해결

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 해시 테이블과 충돌 해결

1. 핵심 요약
- 해시 테이블은 키를 해시 함수에 넣어 저장 위치, 즉 인덱스를 계산하는 자료구조입니다.
- 예를 들어 학번을 넣으면 배열의 특정 칸 번호가 나오고, 그 위치에 데이터를 저장하거나 찾습니다.
- 해시 함수가 값을 고르게 분산시키면 검색·삽입·삭제가 평균적으로 매우 빠릅니다.
- 그래서 평균 시간복잡도는 보통 O(1)로 설명됩니다.
- 하지만 서로 다른 키가 같은 인덱스로 계산될 수 있는데, 이를 충돌이라고 합니다.
- 충돌 해결 방법에는 체이닝과 개방 주소법이 대표적입니다.
- 체이닝은 같은 위치에 여러 데이터를 연결 리스트처럼 이어 저장하는 방식입니다.
- 개방 주소법은 충돌이 나면 다른 빈 칸을 찾아 저장하는 방식입니다.

2. 자주 나오는 포인트
- 해시 테이블의 평균 검색 시간은 O(1), 충돌이 많으면 성능이 떨어질 수 있음
- 충돌 해결: 체이닝, 선형 조사 등 개방 주소법
- 해시 함수는 키를 저장 위치로 변환하는 함수

3. OX 문제
해시 테이블은 키를 해시 함수로 배열 인덱스에 매핑해 평균적으로 빠른 검색을 제공하지만, 서로 다른 키가 같은 인덱스에 매핑되는 충돌이 발생할 수 있다.
O / X?

### 2026-05-03 15:00 - 자료구조 - 힙과 우선순위 큐

지난 문제 정답

정답: O

해설: 패킷 교환은 데이터를 작은 패킷으로 나누고, 각 패킷에 목적지 주소·순서 정보 등을 붙여 전송하는 방식입니다. 회선 교환처럼 전용 경로를 계속 점유하지 않아 여러 사용자가 회선을 효율적으로 공유할 수 있습니다. 다만 패킷별 지연, 손실, 도착 순서 변경이 생길 수 있어 TCP 같은 상위 프로토콜이 재전송과 순서 보장을 담당할 수 있습니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 힙과 우선순위 큐

1. 핵심 요약
- 힙은 완전 이진 트리 형태를 유지하는 자료구조입니다.
- 완전 이진 트리는 마지막 레벨을 제외하고 노드가 채워져 있으며, 마지막 레벨도 왼쪽부터 채워지는 트리입니다.
- 최대 힙은 부모 노드가 자식 노드보다 크거나 같고, 최소 힙은 부모 노드가 자식 노드보다 작거나 같습니다.
- 이 성질 때문에 루트에는 항상 최댓값 또는 최솟값이 위치합니다.
- 우선순위 큐는 먼저 들어온 순서가 아니라 우선순위가 높은 데이터가 먼저 나오는 구조입니다.
- 힙을 사용하면 우선순위가 가장 높은 원소를 빠르게 확인하고 꺼낼 수 있습니다.
- 삽입·삭제 후에는 힙 조건이 깨질 수 있으므로 위로 올리거나 아래로 내리는 재정렬 과정이 필요합니다.
- 힙은 이진 탐색 트리처럼 “왼쪽은 작고 오른쪽은 큼”을 보장하는 구조가 아니라는 점을 구분해야 합니다.

2. 자주 나오는 포인트
- 힙 = 완전 이진 트리 + 부모/자식 간 우선순위 조건
- 최대 힙 루트는 최댓값, 최소 힙 루트는 최솟값
- 우선순위 큐 구현에 자주 사용되며, 정렬 알고리즘인 힙 정렬과도 연결됨

3. OX 문제
힙은 완전 이진 트리 형태를 유지하면서 부모 노드가 자식 노드보다 항상 크거나 작은 성질을 만족하므로, 우선순위가 가장 높은 원소를 빠르게 꺼내는 우선순위 큐 구현에 활용될 수 있다.
O / X?

### 2026-05-04 14:00 - 자료구조 - 퀵 정렬과 피벗 분할

지난 문제 정답

정답: O

해설: CSMA/CD는 전송 중 충돌을 감지하면 전송을 멈추고, 임의의 시간 뒤 다시 전송을 시도하는 방식입니다. 주로 과거 공유 이더넷 환경과 연결됩니다. CSMA/CA는 무선 LAN처럼 충돌 감지가 어려운 환경에서 ACK, 임의 대기, RTS/CTS 등을 이용해 충돌 가능성을 미리 줄이려는 방식입니다. 시험에서는 CD=충돌 감지, CA=충돌 회피로 구분하세요.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 퀵 정렬과 피벗 분할

1. 핵심 요약
- 퀵 정렬은 분할 정복 방식의 대표적인 정렬 알고리즘입니다.
- 먼저 기준값인 피벗을 하나 정하고, 피벗보다 작은 값과 큰 값을 나누어 배치합니다.
- 이렇게 나뉜 왼쪽·오른쪽 구간에 다시 같은 과정을 재귀적으로 적용합니다.
- 분할이 균형 있게 이루어지면 전체 정렬 시간은 평균적으로 O(n log n)에 가깝습니다.
- 하지만 피벗이 계속 최솟값이나 최댓값처럼 한쪽으로 치우치면 분할 효과가 떨어집니다.
- 이 경우 한쪽 부분 배열만 거의 n-1 크기로 남아 최악 시간복잡도는 O(n²)이 됩니다.
- 그래서 퀵 정렬은 평균적으로 빠르지만, 피벗 선택에 따라 성능 차이가 커질 수 있습니다.
- 병합 정렬은 보통 안정적으로 O(n log n)이지만 추가 공간이 필요하다는 점과 비교해 자주 출제됩니다.

2. 자주 나오는 포인트
- 퀵 정렬: 피벗 선택 → 분할 → 재귀 정렬 구조
- 평균 시간복잡도는 O(n log n), 최악 시간복잡도는 O(n²)
- 병합 정렬, 선택 정렬, 삽입 정렬과 시간복잡도 비교 문제로 자주 출제

3. OX 문제
퀵 정렬은 피벗을 기준으로 작은 값과 큰 값의 구간을 나누어 정렬하며, 피벗 선택이 계속 한쪽으로 치우치면 최악 시간복잡도가 O(n²)이 될 수 있다.
O / X?

### 2026-05-04 16:00 - 자료구조 - 병합 정렬과 분할 정복

지난 문제 정답
정답: O

해설: EXISTS는 서브쿼리의 결과 “값”보다 결과 행이 존재하는지를 확인합니다. 서브쿼리가 한 행이라도 반환하면 TRUE, 한 행도 없으면 FALSE입니다. 특히 바깥 쿼리의 각 행마다 관련 데이터가 있는지 확인하는 상관 서브쿼리에서 자주 쓰입니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 병합 정렬과 분할 정복

1. 핵심 요약
- 병합 정렬은 분할 정복 방식의 대표적인 정렬 알고리즘입니다.
- 먼저 배열을 계속 반으로 나누어 더 이상 나눌 수 없는 작은 단위로 만듭니다.
- 그다음 작은 배열들을 정렬된 상태로 다시 합치며 전체 배열을 정렬합니다.
- “나누기”는 대략 log n단계, “합치기”는 각 단계마다 전체 원소 n개를 처리합니다.
- 그래서 평균, 최악 시간복잡도가 모두 O(n log n)으로 안정적입니다.
- 퀵 정렬은 평균적으로 빠르지만 피벗이 나쁘면 최악 O(n²)이 될 수 있는데, 병합 정렬은 최악도 O(n log n)입니다.
- 다만 병합 과정에서 보조 배열이 필요할 수 있어 추가 메모리 사용이 단점으로 나옵니다.
- 같은 값의 상대적 순서를 유지하도록 구현할 수 있어 안정 정렬로도 자주 설명됩니다.

2. 자주 나오는 포인트
- 병합 정렬 = 분할 정복, 반으로 나누고 정렬하며 병합
- 시간복잡도: 평균 O(n log n), 최악 O(n log n)
- 퀵 정렬과 비교: 병합 정렬은 안정적이지만 추가 메모리가 필요할 수 있음

3. OX 문제
병합 정렬은 배열을 계속 반으로 나눈 뒤 정렬된 부분 배열을 합치는 분할 정복 방식이며, 일반적으로 최악 시간복잡도도 O(n log n)이다.
O / X?

### 2026-05-05 17:00 - 자료구조 - 이진 탐색 트리 BST

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 이진 탐색 트리 BST

1. 핵심 요약
- 이진 탐색 트리(BST)는 각 노드가 최대 두 개의 자식을 가지는 이진 트리의 한 종류입니다.
- 핵심 규칙은 “왼쪽 서브트리 값 < 현재 노드 값 < 오른쪽 서브트리 값”입니다.
- 이 규칙 덕분에 값을 찾을 때 전체를 다 보지 않고, 현재 노드와 비교해 왼쪽 또는 오른쪽 한쪽만 탐색할 수 있습니다.
- 예를 들어 찾는 값이 현재 노드보다 작으면 오른쪽은 볼 필요 없이 왼쪽으로 내려갑니다.
- 트리의 균형이 잘 잡혀 있으면 탐색, 삽입, 삭제가 평균적으로 O(log n)에 가깝습니다.
- 하지만 값이 정렬된 순서로 계속 삽입되면 한쪽으로 치우쳐 연결 리스트처럼 변할 수 있고, 이때는 O(n)까지 느려질 수 있습니다.
- BST를 중위 순회하면 값이 오름차순으로 출력된다는 점도 자주 나옵니다.

2. 자주 나오는 포인트
- BST 규칙: 왼쪽은 작고, 오른쪽은 크다.
- 중위 순회 결과는 오름차순 정렬 형태가 된다.
- 균형 트리는 O(log n), 편향 트리는 O(n)까지 성능 저하 가능.

3. OX 문제
이진 탐색 트리에서는 어떤 노드의 왼쪽 서브트리에 있는 모든 값이 그 노드보다 작고, 오른쪽 서브트리에 있는 모든 값이 그 노드보다 크다는 규칙을 이용해 탐색 범위를 줄일 수 있다.
O / X?

### 2026-05-06 19:00 - 자료구조 - 그래프 표현 인접 행렬과 인접 리스트

찬혁 님께 보낼 메시지:

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 그래프 표현 인접 행렬과 인접 리스트

지난 문제 정답
정답: O

해설: ALL은 서브쿼리 결과의 모든 값과 비교 조건을 만족해야 TRUE가 됩니다. 예를 들어 A > ALL(서브쿼리)은 A가 서브쿼리에서 나온 모든 값보다 커야 참입니다. 반대로 ANY/SOME은 하나라도 조건을 만족하면 TRUE입니다.

1. 핵심 요약
- 그래프는 정점(Vertex)과 간선(Edge)으로 구성된 자료구조입니다.
- 그래프를 컴퓨터에 저장하는 대표 방식은 인접 행렬과 인접 리스트입니다.
- 인접 행렬은 정점 수가 V일 때 V×V 배열로 간선 존재 여부를 표시합니다.
- 두 정점 사이에 간선이 있는지 바로 확인할 수 있어 접근이 빠릅니다.
- 하지만 간선이 적은 희소 그래프에서는 대부분 칸이 비어 공간 낭비가 큽니다.
- 인접 리스트는 각 정점마다 연결된 정점 목록만 저장합니다.
- 간선이 적은 그래프에서는 인접 리스트가 공간 효율적입니다.
- 단, 특정 두 정점 사이의 간선 존재 여부를 확인하려면 리스트를 탐색해야 할 수 있습니다.

2. 자주 나오는 포인트
- 인접 행렬: 간선 확인 빠름, 공간복잡도 O(V²)
- 인접 리스트: 희소 그래프에 유리, 인접 정점 순회에 효율적
- BFS/DFS 구현 시 인접 리스트를 자주 사용하지만, 그래프가 조밀하면 인접 행렬도 고려 가능

3. OX 문제
인접 행렬은 정점 수가 V일 때 V×V 크기의 배열로 간선 존재 여부를 표현하므로, 두 정점 사이의 간선 존재 확인은 빠르지만 간선이 적은 그래프에서는 공간 낭비가 생길 수 있다.
O / X?

### 2026-05-07 15:00 - 자료구조 - 최소 신장 트리와 크루스칼 알고리즘

지난 문제 정답

정답: O

해설: DNS 재귀 질의는 클라이언트가 로컬 DNS 서버에게 최종 답을 요청하면, 로컬 DNS 서버가 여러 DNS 서버를 대신 조회해 최종 IP 주소를 찾아 돌려주는 방식입니다. 반복 질의는 질의를 받은 DNS 서버가 최종 답을 모를 때 “다음 서버에 물어보라”는 식으로 안내하는 방식입니다. 실제로는 PC가 로컬 DNS에 재귀 질의를 하고, 로컬 DNS가 루트/TLD/권한 DNS에 반복 질의를 수행하는 흐름으로 이해하면 좋습니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 최소 신장 트리와 크루스칼 알고리즘

1. 핵심 요약
- 최소 신장 트리(MST)는 가중치가 있는 무방향 연결 그래프에서 모든 정점을 연결하면서 간선 가중치 합이 최소가 되는 트리입니다.
- “신장”은 모든 정점을 포함한다는 뜻이고, “트리”이므로 사이클이 없어야 합니다.
- 정점이 V개라면 최소 신장 트리의 간선 수는 항상 V-1개입니다.
- 크루스칼 알고리즘은 간선을 가중치가 작은 순서대로 정렬한 뒤 하나씩 선택하는 탐욕 알고리즘입니다.
- 단, 선택했을 때 사이클이 생기는 간선은 제외합니다.
- 사이클 여부는 보통 Union-Find, 즉 서로소 집합 자료구조로 판단합니다.
- 프림 알고리즘이 하나의 정점에서 시작해 트리를 확장하는 방식이라면, 크루스칼은 간선 전체를 정렬해 고르는 방식입니다.
- 시험에서는 “최소 비용으로 전체 네트워크 연결” 같은 표현이 나오면 MST를 떠올리면 좋습니다.

2. 자주 나오는 포인트
- MST 조건: 모든 정점 포함, 사이클 없음, 간선 수 V-1개, 가중치 합 최소
- 크루스칼: 간선 중심, 가중치 오름차순 정렬, 사이클 제외
- 프림과 비교: 프림은 정점 중심 확장, 크루스칼은 간선 중심 선택

3. OX 문제
크루스칼 알고리즘은 간선을 가중치 오름차순으로 정렬한 뒤, 사이클을 만들지 않는 간선을 선택해 최소 신장 트리를 구성할 수 있다.
O / X?

### 2026-05-08 19:00 - 자료구조 - 다익스트라 최단 경로 알고리즘

지난 문제 정답
정답: O

해설: VPN은 공용 네트워크 위에 터널을 만들고, 암호화·인증을 통해 사설망처럼 안전한 통신을 돕는 기술입니다. NAT는 사설 IP와 공인 IP 사이의 주소를 변환하는 기술이므로 VPN과 목적이 다릅니다. 시험에서는 VPN=터널링·암호화, NAT=주소 변환, 프록시=중계/캐시로 구분하세요.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 다익스트라 최단 경로 알고리즘

1. 핵심 요약
- 다익스트라 알고리즘은 하나의 시작 정점에서 다른 모든 정점까지의 최단 거리를 구하는 알고리즘입니다.
- 핵심 아이디어는 “현재까지 거리가 가장 짧은 정점부터 확정한다”는 탐욕적 선택입니다.
- 시작 정점의 거리는 0, 나머지는 무한대로 두고 출발합니다.
- 이후 가장 가까운 미확정 정점을 선택하고, 그 정점을 거쳐 다른 정점으로 가는 거리가 더 짧으면 갱신합니다.
- 우선순위 큐를 쓰면 매번 가장 짧은 거리의 정점을 효율적으로 꺼낼 수 있습니다.
- 단, 다익스트라는 음수 가중치 간선이 없는 그래프에서 사용해야 합니다.
- 음수 간선이 있으면 나중에 더 짧은 경로가 발견되어 이미 확정한 결과가 틀릴 수 있습니다.
- 음수 가중치가 있는 최단 경로 문제는 벨만-포드 알고리즘과 비교해서 기억하면 좋습니다.

2. 자주 나오는 포인트
- 다익스트라: 단일 출발점 최단 경로, 음수 가중치 간선이 없을 때 사용
- 우선순위 큐를 활용하면 효율적으로 구현 가능
- 벨만-포드: 음수 간선 처리 가능, 음수 사이클 탐지도 가능

3. OX 문제
다익스트라 알고리즘은 시작 정점에서 각 정점까지의 최단 거리를 구할 수 있지만, 음수 가중치 간선이 있는 그래프에서도 항상 올바른 최단 거리를 보장한다.
O / X?

### 2026-05-10 13:00 - 자료구조 - 버블 정렬과 시간복잡도

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 버블 정렬과 시간복잡도

1. 핵심 요약
- 버블 정렬은 인접한 두 원소를 비교해 순서가 맞지 않으면 서로 교환하는 정렬 방식입니다.
- 한 번 전체를 훑으면 가장 큰 값이 맨 뒤로 이동하는 식으로 정렬 범위가 줄어듭니다.
- 동작 원리가 단순해서 구현은 쉽지만, 비교와 교환을 반복하므로 효율은 낮은 편입니다.
- 평균 시간복잡도와 최악 시간복잡도는 O(n²)입니다.
- 이미 정렬된 배열에서 교환이 한 번도 없으면 종료하는 최적화를 넣으면 최선은 O(n)이 될 수 있습니다.
- 선택 정렬은 “최솟값을 찾아 교환”, 삽입 정렬은 “앞쪽 정렬 구간에 끼워 넣기”, 버블 정렬은 “인접 원소 비교·교환”으로 구분하면 좋습니다.

2. 자주 나오는 포인트
- 버블 정렬: 인접 원소 비교 후 교환, 평균/최악 O(n²)
- 단순 정렬인 선택·삽입·버블 정렬은 대체로 O(n²) 계열로 자주 비교됩니다.
- “항상 O(n log n)” 같은 표현은 버블 정렬에는 맞지 않습니다.

3. OX 문제
버블 정렬은 인접한 두 원소를 비교해 필요하면 교환하는 과정을 반복하는 정렬이며, 평균 시간복잡도는 O(n²)이다.
O / X?

### 2026-05-10 17:00 - 자료구조 - 위상 정렬과 DAG

지난 문제 정답
정답: O

해설: TTL은 IP 패킷이 네트워크에서 무한히 떠도는 것을 막는 수명 값입니다. 라우터를 한 홉 지날 때마다 보통 1씩 감소하고, 0이 되면 패킷을 폐기합니다. 이 원리는 라우팅 루프 방지와 traceroute 동작에도 연결됩니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 위상 정렬과 DAG

1. 핵심 요약
- 위상 정렬은 방향 그래프에서 “먼저 해야 하는 것”이 앞에 오도록 정점을 나열하는 방법입니다.
- 대표 예시는 선수 과목, 작업 순서, 빌드 의존성입니다.
- 위상 정렬은 방향 비순환 그래프, 즉 DAG에서만 가능합니다.
- DAG는 Directed Acyclic Graph의 약자로, 방향은 있지만 사이클은 없는 그래프입니다.
- 보통 진입 차수가 0인 정점부터 선택합니다.
- 진입 차수는 어떤 정점으로 들어오는 간선의 개수입니다.
- 선택한 정점을 결과에 넣고, 그 정점에서 나가는 간선을 제거하며 다음 진입 차수 0 정점을 찾습니다.
- 가능한 순서가 하나로 고정되지 않을 수 있으며, 조건만 만족하면 여러 정답이 가능합니다.

2. 자주 나오는 포인트
- 위상 정렬은 DAG에서만 가능하며, 사이클이 있으면 전체 정렬이 불가능합니다.
- 진입 차수 0인 정점을 큐에 넣는 방식이 자주 출제됩니다.
- 결과가 여러 개 나올 수 있으므로 “항상 유일하다”는 표현은 주의해야 합니다.

3. OX 문제
위상 정렬은 방향 그래프에서 선후 관계를 만족하도록 정점을 나열하는 방법이며, 그래프에 사이클이 있으면 모든 정점을 정상적으로 위상 정렬할 수 없다.
O / X?

### 2026-05-11 20:00 - 자료구조 - AVL 트리와 균형 회전

지난 문제 정답

정답: O

해설: L2 스위치는 데이터 링크 계층 장비라서 MAC 주소와 MAC 주소 테이블을 보고 같은 LAN 안에서 프레임을 전달합니다. 라우터는 네트워크 계층 장비라서 목적지 IP 주소와 라우팅 테이블을 보고 서로 다른 네트워크 사이의 다음 홉을 결정합니다. 시험에서는 스위치=MAC·2계층·프레임, 라우터=IP·3계층·패킷 전달로 구분하면 됩니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - AVL 트리와 균형 회전

1. 핵심 요약
- AVL 트리는 이진 탐색 트리(BST)의 한 종류입니다.
- 일반 BST는 작은 값은 왼쪽, 큰 값은 오른쪽에 두어 탐색을 빠르게 합니다.
- 하지만 데이터가 한쪽으로 치우쳐 삽입되면 트리가 거의 연결 리스트처럼 변해 탐색 성능이 O(n)까지 나빠질 수 있습니다.
- AVL 트리는 이런 문제를 막기 위해 각 노드의 왼쪽·오른쪽 서브트리 높이 차이를 관리합니다.
- 이 높이 차이를 균형 인수라고 하며, 보통 -1, 0, 1 범위를 유지해야 합니다.
- 삽입이나 삭제 후 균형이 깨지면 회전을 수행해 트리 높이를 다시 맞춥니다.
- 회전 유형은 LL, RR, LR, RL로 구분하며, 단일 회전 또는 이중 회전이 사용됩니다.
- 균형을 유지하기 때문에 탐색·삽입·삭제를 최악의 경우에도 O(log n) 수준으로 유지할 수 있습니다.

2. 자주 나오는 포인트
- AVL 트리 = 자가 균형 이진 탐색 트리입니다.
- 균형 인수는 왼쪽 서브트리와 오른쪽 서브트리의 높이 차이입니다.
- 일반 BST는 한쪽으로 치우치면 O(n), AVL은 회전으로 균형을 맞춰 O(log n)을 유지합니다.

3. OX 문제
AVL 트리는 이진 탐색 트리의 한 종류로, 삽입이나 삭제 후 균형 인수가 허용 범위를 벗어나면 회전을 통해 트리 높이를 조정한다.
O / X?

### 2026-05-13 17:00 - 자료구조 - 퀵 정렬의 평균·최악 시간복잡도

지난 문제 정답
정답: O

해설: 솔트는 비밀번호 해시 전에 함께 섞는 사용자별 임의값입니다. 같은 비밀번호라도 솔트가 다르면 해시 입력값이 달라져 저장 해시도 달라질 수 있습니다. 그래서 미리 만들어 둔 해시 목록인 레인보우 테이블 공격을 어렵게 만듭니다. 단, 솔트는 비밀키라기보다 중복 해시를 막는 보조값이고, 실제 비밀번호 저장에는 bcrypt, scrypt, Argon2처럼 느린 해시 함수를 쓰는 것이 좋습니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 퀵 정렬의 평균·최악 시간복잡도

1. 핵심 요약
- 퀵 정렬은 하나의 기준값인 피벗을 정한 뒤, 피벗보다 작은 값과 큰 값을 나누어 정렬하는 분할 정복 알고리즘입니다.
- 나뉜 왼쪽 구간과 오른쪽 구간에도 같은 과정을 재귀적으로 반복합니다.
- 분할이 균형 있게 이루어지면 각 단계에서 전체 원소를 훑고, 단계 수가 대략 log n이 되어 평균 시간복잡도는 O(n log n)입니다.
- 하지만 피벗이 계속 최솟값이나 최댓값처럼 한쪽 끝으로 치우치면, 한쪽 부분 배열만 거의 n-1 크기로 남습니다.
- 이 경우 비교가 많이 반복되어 최악 시간복잡도는 O(n²)이 될 수 있습니다.
- 그래서 퀵 정렬은 평균적으로 빠르지만, 피벗 선택 방식이 성능에 큰 영향을 줍니다.
- 병합 정렬은 추가 메모리가 필요할 수 있지만 최악도 O(n log n)이라는 점에서 자주 비교됩니다.

2. 자주 나오는 포인트
- 퀵 정렬: 피벗, 분할 정복, 평균 O(n log n), 최악 O(n²)
- 피벗 선택이 한쪽으로 치우치면 성능이 급격히 나빠질 수 있음
- 병합 정렬과 비교: 병합 정렬은 최악 O(n log n), 퀵 정렬은 평균은 빠르지만 최악 O(n²)

3. OX 문제
퀵 정렬은 평균적으로 빠른 정렬로 알려져 있지만, 피벗 선택이 계속 한쪽으로 치우치면 최악 시간복잡도가 O(n²)이 될 수 있다.
O / X?

### 2026-05-14 15:00 - 자료구조 - 플로이드-워셜 모든 쌍 최단 경로

지난 문제 정답
정답: X

해설: DEFAULT는 INSERT할 때 해당 컬럼 값을 생략한 경우에 기본값을 넣기 위한 설정입니다. 하지만 명시적으로 NULL을 넣으면 일반적으로 “값을 생략한 것”이 아니라 NULL을 넣은 것으로 처리되므로, 항상 DEFAULT로 자동 대체된다고 볼 수 없습니다. NULL을 막고 싶다면 NOT NULL 제약조건을 함께 고려해야 합니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 플로이드-워셜 모든 쌍 최단 경로

1. 핵심 요약
- 플로이드-워셜 알고리즘은 모든 정점 쌍 사이의 최단 거리를 구하는 알고리즘입니다.
- 다익스트라가 “한 시작점에서 다른 정점들까지”를 구한다면, 플로이드-워셜은 “모든 출발점과 모든 도착점 조합”을 한 번에 계산합니다.
- 기본 구조는 거리 행렬을 만들고, 중간에 거쳐 갈 정점 k를 하나씩 허용하면서 더 짧은 경로가 있는지 갱신하는 방식입니다.
- 핵심 점화식은 dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]) 입니다.
- 동적 계획법 관점에서 “k번 정점까지를 중간 경유지로 사용할 때의 최단 거리”를 단계적으로 확장한다고 이해하면 좋습니다.
- 시간복잡도는 O(V³)이므로 정점 수가 많으면 부담이 큽니다.
- 음수 간선이 있어도 사용할 수 있지만, 음수 사이클이 있으면 최단 거리 자체가 의미 없어질 수 있습니다.

2. 자주 나오는 포인트
- 플로이드-워셜: 모든 정점 쌍 최단 경로, O(V³), 거리 행렬 사용
- 다익스트라: 한 시작점 기준 최단 경로, 음수 간선에 취약
- 점화식 dist[i][j] > dist[i][k] + dist[k][j] 이면 갱신

3. OX 문제
플로이드-워셜 알고리즘은 모든 정점 쌍 사이의 최단 거리를 구하는 동적 계획법 알고리즘이며, 중간 정점을 하나씩 허용해 가며 거리 행렬을 갱신한다.
O / X?

### 2026-05-15 20:00 - 자료구조 - 삽입 정렬과 거의 정렬된 데이터

찬혁 님께 보낼 학습 메시지입니다.

지난 문제 정답
정답: O

해설: 브로드캐스트는 같은 브로드캐스트 도메인 안의 모든 장비에게 전달되는 통신 방식입니다. ARP 요청처럼 목적지 MAC 주소를 모를 때 자주 쓰이며, 스위치는 같은 VLAN 안에서 브로드캐스트를 전달할 수 있습니다. 반면 라우터는 일반적으로 2계층 브로드캐스트를 다른 네트워크로 넘기지 않으므로 브로드캐스트 도메인을 분리합니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - 삽입 정렬과 거의 정렬된 데이터

1. 핵심 요약
- 삽입 정렬은 앞쪽을 “이미 정렬된 구간”으로 보고, 다음 원소를 알맞은 위치에 끼워 넣는 정렬입니다.
- 두 번째 원소부터 시작해 왼쪽의 정렬된 값들과 비교하며 자기 자리를 찾습니다.
- 필요한 경우 큰 값들을 오른쪽으로 한 칸씩 밀고, 빈자리에 현재 원소를 삽입합니다.
- 평균과 최악 시간복잡도는 O(n²)입니다.
- 하지만 데이터가 이미 거의 정렬되어 있으면 이동할 거리가 짧아져 매우 효율적일 수 있습니다.
- 최선의 경우, 즉 이미 정렬된 배열에서는 비교만 조금 하고 거의 이동하지 않아 O(n)에 가깝게 동작합니다.
- 구현이 단순하고, 같은 값의 상대적 순서를 유지할 수 있어 안정 정렬로 볼 수 있습니다.

2. 자주 나오는 포인트
- 삽입 정렬: 정렬된 부분에 현재 원소를 끼워 넣는 방식
- 시간복잡도: 평균·최악 O(n²), 거의 정렬된 경우 유리
- 선택 정렬은 최솟값을 찾아 교환, 삽입 정렬은 현재 값을 적절한 위치에 삽입

3. OX 문제
삽입 정렬은 앞쪽의 이미 정렬된 부분에 현재 원소를 알맞은 위치에 끼워 넣는 방식이며, 데이터가 거의 정렬되어 있으면 비교와 이동이 줄어들어 효율적일 수 있다.
O / X?

### 2026-05-18 16:00 - 자료구조 - DFS와 스택/재귀 구현

지난 문제 정답
정답: O

해설: 체크포인트는 DB 회복 시 기준점 역할을 합니다. 장애가 발생하면 DBMS가 처음부터 모든 로그를 확인하지 않고, 최근 체크포인트 이후 로그를 중심으로 REDO/UNDO 대상을 판단할 수 있어 복구 시간을 줄입니다. COMMIT된 변경은 필요하면 REDO, COMMIT되지 않은 변경은 UNDO로 처리합니다.

[정보처리기사/전산직 필기]
오늘 주제: 자료구조 - DFS와 스택/재귀 구현

1. 핵심 요약
- DFS는 Depth-First Search, 즉 깊이 우선 탐색입니다.
- 시작 정점에서 한 경로를 따라 가능한 깊게 들어간 뒤, 더 갈 곳이 없으면 되돌아옵니다.
- 이 “되돌아오기” 구조 때문에 스택과 잘 맞습니다.
- 직접 스택 자료구조를 사용할 수도 있고, 재귀 호출의 함수 호출 스택을 이용할 수도 있습니다.
- 그래프에 사이클이 있으면 같은 정점을 반복 방문할 수 있으므로 방문 배열이 필요합니다.
- DFS는 경로 탐색, 연결 요소 확인, 사이클 탐지, 백트래킹 문제와 자주 연결됩니다.
- BFS가 가까운 정점부터 넓게 탐색한다면, DFS는 한 방향으로 깊게 파고드는 방식입니다.

2. 자주 나오는 포인트
- DFS: 스택 또는 재귀, 깊이 우선 탐색
- BFS: 큐, 시작점에서 가까운 정점부터 탐색
- 그래프 탐색 문제에서는 방문 처리 시점과 중복 방문 방지가 중요합니다.

3. OX 문제
DFS는 시작 정점에서 한 방향으로 가능한 깊게 탐색한 뒤 더 갈 곳이 없으면 되돌아오며, 재귀 호출이나 스택을 이용해 구현할 수 있다.
O / X?

### 2026-05-20 16:00 - 자료구조 - 버블 정렬과 인접 교환

[정보처리기사/전산직 필기]

지난 문제 정답
정답: O

해설: 인터럽트는 CPU가 현재 실행 중인 작업을 잠시 멈추고 운영체제의 인터럽트 서비스 루틴(ISR)으로 제어를 넘기게 하는 신호입니다. 입출력 완료, 타이머, 예외 상황 처리 등에 활용됩니다. CPU는 현재 문맥을 저장한 뒤 ISR을 처리하고, 끝나면 문맥을 복원해 원래 작업을 이어갈 수 있습니다. 폴링처럼 CPU가 계속 장치 상태를 확인하는 방식과 구분하세요.

오늘 주제: 자료구조 - 버블 정렬과 인접 교환

1. 핵심 요약
- 버블 정렬은 서로 인접한 두 원소를 비교해 순서가 맞지 않으면 교환하는 정렬 방식입니다.
- 오름차순 기준으로 큰 값이 비교와 교환을 거치며 점점 오른쪽으로 밀려납니다.
- 한 회전이 끝나면 가장 큰 값 하나가 배열의 뒤쪽에 확정될 수 있습니다.
- 구현이 매우 단순해서 정렬 알고리즘의 기본 원리를 이해하기 좋습니다.
- 하지만 평균과 최악 시간복잡도는 O(n²)이라 데이터가 많으면 비효율적입니다.
- 교환이 많이 발생할 수 있어 실제 성능은 삽입 정렬보다 불리한 경우가 많습니다.
- 인접한 원소끼리만 교환하므로 같은 값의 상대적 순서를 유지할 수 있어 안정 정렬로 볼 수 있습니다.

2. 자주 나오는 포인트
- 버블 정렬: 인접 비교 + 필요 시 교환.
- 시간복잡도: 평균/최악 O(n²), 최선은 최적화 여부에 따라 O(n) 가능.
- 선택 정렬은 최솟값/최댓값을 찾아 교환, 삽입 정렬은 정렬된 부분에 끼워 넣기라는 차이를 구분하세요.

3. OX 문제
버블 정렬은 인접한 원소를 비교해 필요한 경우 교환하는 과정을 반복하며, 한 회전이 끝날 때마다 가장 큰 값이 뒤쪽으로 이동할 수 있다.
O / X?
