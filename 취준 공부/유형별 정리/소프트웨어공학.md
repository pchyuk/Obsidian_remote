# 소프트웨어공학 유형별 정리

## 개요

- 마지막 업데이트: 2026-05-02 11:49 KST
- 누적 정리 수: 6개
- 출처: 매일 정처기/전산직 학습 cron 요약

---

## 2026-04

### 2026-04-26 20:30 - 소프트웨어공학 - 결합도와 응집도

[정보처리기사/전산직 필기]
오늘 주제: 소프트웨어공학 - 결합도와 응집도

지난 문제 정답
정답: X

해설: 제3정규형(3NF)은 2NF를 만족한 뒤, 이행적 함수 종속까지 제거해야 합니다. 즉 기본키가 아닌 속성이 다른 기본키가 아닌 속성을 통해 간접적으로 결정되면 안 됩니다. 부분 함수 종속 제거만으로는 2NF 수준입니다.

1. 핵심 요약
- 모듈화는 큰 소프트웨어를 기능 단위로 나누어 이해와 수정이 쉽도록 만드는 설계 기법입니다.
- 좋은 모듈 설계의 핵심 기준은 응집도는 높게, 결합도는 낮게 만드는 것입니다.
- 응집도는 한 모듈 안의 구성 요소들이 하나의 목적을 위해 얼마나 밀접하게 관련되어 있는지를 뜻합니다.
- 응집도가 높으면 모듈이 맡은 역할이 분명해져 수정 범위가 작아지고 재사용도 쉬워집니다.
- 결합도는 모듈과 모듈 사이의 의존 정도를 의미합니다.
- 결합도가 높으면 한 모듈의 변경이 다른 모듈에 연쇄적으로 영향을 주기 쉬워 유지보수가 어려워집니다.
- 그래서 설계할 때는 내부적으로는 관련 기능을 모으고, 외부와는 필요한 인터페이스만 통해 느슨하게 연결하는 것이 좋습니다.

2. 자주 나오는 포인트
- 좋은 설계 원칙: 고응집, 저결합
- 응집도는 기능적 응집이 가장 좋고, 우연적 응집이 가장 나쁩니다.
- 결합도는 자료 결합이 비교적 좋고, 내용 결합이 가장 나쁩니다.

3. OX 문제
좋은 모듈 설계에서는 모듈 내부 기능들이 서로 밀접하게 관련될수록 응집도가 높고, 다른 모듈에 덜 의존할수록 결합도가 낮다.
O / X?

### 2026-04-27 15:00 - 소프트웨어공학 - 블랙박스 테스트와 화이트박스 테스트

[정보처리기사/전산직 필기]
오늘 주제: 소프트웨어공학 - 블랙박스 테스트와 화이트박스 테스트

1. 핵심 요약
- 소프트웨어 테스트는 프로그램이 요구사항대로 동작하는지, 결함이 숨어 있지 않은지 확인하는 과정입니다.
- 블랙박스 테스트는 프로그램 내부 구조를 보지 않고, 입력값과 출력값이 명세와 맞는지 검사합니다.
- 즉 “안에서 어떻게 처리하는가”보다 “요구한 기능을 제대로 수행하는가”에 초점을 둡니다.
- 대표 기법으로 동등 분할, 경계값 분석, 원인-결과 그래프, 오류 예측 등이 있습니다.
- 화이트박스 테스트는 소스 코드, 제어 흐름, 조건문, 반복문 같은 내부 구조를 기준으로 검사합니다.
- 문장 검증, 분기 검증, 조건 검증, 경로 검증 등이 화이트박스 테스트와 관련됩니다.
- 실무와 시험 모두에서 블랙박스는 기능/명세 중심, 화이트박스는 구조/코드 중심으로 구분하는 것이 핵심입니다.
- 테스트는 오류가 없음을 증명하는 것이 아니라, 오류를 발견하기 위한 활동이라는 점도 자주 나옵니다.

2. 자주 나오는 포인트
- 블랙박스 테스트: 명세 기반, 기능 테스트, 동등 분할·경계값 분석
- 화이트박스 테스트: 구조 기반, 코드 논리 검사, 경로·분기·조건 검증
- “내부 구조를 모른다/본다”가 두 테스트를 가르는 핵심 기준입니다.

3. OX 문제
블랙박스 테스트는 프로그램의 내부 제어 구조나 소스 코드를 기준으로 모든 경로를 검사하는 테스트 기법이다.
O / X?

### 2026-04-28 12:00 - 소프트웨어공학 - UML 유스케이스 다이어그램

[정보처리기사/전산직 필기]
오늘 주제: 소프트웨어공학 - UML 유스케이스 다이어그램

1. 핵심 요약
- UML은 소프트웨어 시스템을 시각적으로 표현하기 위한 표준 모델링 언어입니다.
- 그중 유스케이스 다이어그램은 “시스템이 사용자에게 어떤 기능을 제공하는가”를 보여줍니다.
- 여기서 사용자는 액터라고 부르며, 실제 사람뿐 아니라 외부 시스템도 액터가 될 수 있습니다.
- 유스케이스는 액터가 시스템을 통해 수행하려는 기능이나 서비스를 의미합니다.
- 예를 들어 “회원이 로그인한다”, “관리자가 게시글을 삭제한다” 같은 기능이 유스케이스입니다.
- 유스케이스 다이어그램은 요구사항 분석 단계에서 자주 사용됩니다.
- 중요한 점은 내부 로직이나 클래스 구조를 설명하는 도구가 아니라는 것입니다.
- 내부 설계는 클래스 다이어그램, 시퀀스 다이어그램 등에서 더 자세히 다룹니다.

2. 자주 나오는 포인트
- 유스케이스 다이어그램: 액터와 시스템 기능 중심
- 클래스 다이어그램: 클래스, 속성, 메서드, 관계 중심
- 요구사항 분석 단계에서 “사용자 관점의 기능”을 파악할 때 유용

3. OX 문제
UML의 유스케이스 다이어그램은 사용자가 시스템으로부터 어떤 기능을 제공받는지를 표현하는 데 초점을 두며, 시스템 내부 클래스의 속성과 메서드 구조를 상세히 나타내는 다이어그램은 아니다.

O / X?

### 2026-04-29 14:00 - 소프트웨어공학 - 디자인 패턴: 싱글톤

지난 문제 정답

정답: O

해설: 스레드는 프로세스 안에서 실행되는 더 작은 실행 단위입니다. 같은 프로세스의 스레드들은 코드, 데이터, 힙 영역을 공유하지만, 각 스레드는 자기 스택과 실행 흐름을 따로 가집니다. 그래서 스레드는 프로세스보다 문맥 교환 비용이 작고 통신이 쉽지만, 한 스레드의 오류가 같은 프로세스 안의 다른 스레드에 영향을 줄 수 있습니다.

[정보처리기사/전산직 필기]
오늘 주제: 소프트웨어공학 - 디자인 패턴: 싱글톤

1. 핵심 요약
- 디자인 패턴은 자주 반복되는 설계 문제에 대한 검증된 해결 방식입니다.
- 싱글톤 패턴은 특정 클래스의 객체가 프로그램 전체에서 하나만 생성되도록 제한하는 패턴입니다.
- 보통 생성자를 외부에서 직접 호출하지 못하게 막고, 정적 메서드를 통해 유일한 인스턴스를 반환합니다.
- 예를 들어 로그 관리자, 환경 설정 관리자, 공용 캐시처럼 하나만 있어야 자연스러운 객체에 사용됩니다.
- 싱글톤은 객체 생성 방식을 다루므로 생성 패턴에 속합니다.
- 장점은 인스턴스 중복 생성을 막고, 전역적으로 같은 객체에 접근할 수 있다는 점입니다.
- 단점은 전역 상태가 강해져 테스트가 어려워지고, 객체 간 결합도가 높아질 수 있다는 점입니다.
- 시험에서는 “인스턴스 하나 보장”, “전역 접근점 제공”, “생성 패턴”을 함께 기억하면 좋습니다.

2. 자주 나오는 포인트
- 싱글톤은 생성 패턴이며, 클래스의 인스턴스를 하나로 제한한다.
- 전역 접근은 편리하지만, 남용하면 결합도 증가와 테스트 어려움이 생길 수 있다.
- 팩토리 메서드, 추상 팩토리, 빌더도 생성 패턴으로 함께 구분해두면 좋다.

3. OX 문제
싱글톤 패턴은 특정 클래스의 인스턴스가 하나만 생성되도록 보장하고, 전역적으로 접근할 수 있는 지점을 제공하는 생성 패턴이다.
O / X?

### 2026-04-30 13:00 - 소프트웨어공학 - UML 클래스 다이어그램 관계

[정보처리기사/전산직 필기]
오늘 주제: 소프트웨어공학 - UML 클래스 다이어그램 관계

1. 핵심 요약
- UML 클래스 다이어그램은 시스템을 구성하는 클래스, 속성, 메서드, 클래스 간 관계를 정적으로 표현하는 다이어그램입니다.
- 클래스는 보통 이름, 속성, 연산의 3칸 구조로 표현합니다.
- 관계에는 연관, 의존, 일반화, 실체화, 집합, 합성 등이 있습니다.
- 일반화는 부모-자식 클래스의 상속 관계를 뜻합니다.
- 실체화는 인터페이스를 클래스가 구현하는 관계입니다.
- 집합과 합성은 전체-부분 관계를 나타내지만, 합성이 더 강한 생명주기 종속성을 가집니다.
- 예를 들어 “자동차-바퀴”처럼 부분이 전체에 강하게 종속되면 합성으로 볼 수 있습니다.
- 시험에서는 관계 이름보다 “기호와 의미 매칭”이 자주 나옵니다.

2. 자주 나오는 포인트
- 일반화: 상속 관계, 속이 빈 삼각형 화살표
- 집합: 약한 전체-부분 관계, 속이 빈 마름모
- 합성: 강한 전체-부분 관계, 속이 찬 마름모

3. OX 문제
UML 클래스 다이어그램에서 일반화 관계는 전체 객체와 부분 객체의 포함 관계를 나타내며, 속이 빈 마름모로 표현한다.
O / X?

---

## 2026-05

### 2026-05-01 13:00 - 소프트웨어공학 - 요구사항 검증과 명세서 품질

지난 문제 정답
정답: O

해설: 함수 종속 A → B는 A 값이 정해지면 B 값도 하나로 정해진다는 뜻입니다. 같은 A 값을 가진 튜플들이 서로 다른 B 값을 가지면 함수 종속은 성립하지 않습니다. 후보키는 튜플을 유일하게 식별하는 최소 속성 집합이므로, 릴레이션의 모든 속성을 함수적으로 결정할 수 있습니다.

[정보처리기사/전산직 필기]
오늘 주제: 소프트웨어공학 - 요구사항 검증과 명세서 품질

1. 핵심 요약
- 요구사항 검증은 작성된 요구사항 명세서가 사용자 요구를 정확히 반영했는지 확인하는 활동입니다.
- 개발이 모두 끝난 뒤에만 하는 것이 아니라, 분석·명세 단계 이후 초기에 수행해 오류 비용을 줄이는 데 목적이 있습니다.
- 요구사항 오류는 뒤늦게 발견될수록 수정 비용이 커지므로, 초반 검증이 중요합니다.
- 좋은 요구사항은 완전성, 일관성, 명확성, 추적성, 검증 가능성을 갖춰야 합니다.
- 완전성은 필요한 요구가 빠지지 않은 상태입니다.
- 일관성은 요구사항끼리 서로 모순되지 않는 상태입니다.
- 명확성은 여러 의미로 해석되지 않도록 구체적으로 쓰인 상태입니다.
- 검증 기법에는 요구사항 검토, 동료 검토, 워크스루, 인스펙션, 프로토타이핑 등이 있습니다.

2. 자주 나오는 포인트
- 요구사항 단계: 도출 → 분석 → 명세 → 확인/검증 순서로 자주 출제됩니다.
- 요구사항 검증은 “완성된 프로그램 테스트”가 아니라 “명세서가 올바른지 확인”하는 활동입니다.
- 명세서 품질 기준: 완전성, 일관성, 명확성, 추적성, 검증 가능성을 구분하세요.

3. OX 문제
요구사항 검증은 개발이 끝난 뒤에만 수행하며, 요구사항 명세서의 완전성·일관성·명확성을 확인하는 활동과는 관련이 없다.
O / X?
